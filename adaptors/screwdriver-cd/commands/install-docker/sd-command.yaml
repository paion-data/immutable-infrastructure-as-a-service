namespace: paion-data
name: install-docker
version: '1.0.0'
description: Install Docker compatible on all OS platforms, including Unix and Linux
usage: |
  sd-cmd exec paion-data/install-docker@latest
maintainer: jack20220723@gmail.com
format: binary
binary:
  file: ./run.sh
